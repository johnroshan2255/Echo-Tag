# Origin of the Colyseus server. Used for the <link rel="preconnect"> in index.html so the
# websocket handshake overlaps the engine chunk download instead of waiting behind it.
VITE_WS_ORIGIN=http://localhost:2567
